<!DOCTYPE html>
<html lang="jp" dir="ltr">

<head>
    <meta charset="utf-8">
    <style>
        .container {
            width: 90%;
        }

        th {
         padding: 5px 20px;
        }

        td{
         padding: 0 10px;
        }

        span{
         padding-right:20px;
        }

        .submit{
         padding-top: 20px
        }
    </style>
</head>


<div class="container ">
 <h3><%= @customer.last_name %><%= @customer.first_name %>さんの会員情報編集</h2>
<div class="row">
<%= form_with model: @customer, url: {controller: 'admin/customers', action: 'update' }, local:true do |f| %>
<div class="col-12">
 <table>
   <tr>
     <th>会員ID</th>
     <td><%= @customer.id %></td>
   </tr>
   <tr>
     <th>氏名</th>
     <td><span><%= f.text_field :last_name %></span><%= f.text_field :first_name %></td>
     <td></td>
   </tr>
   <tr>
     <th>フリガナ</th>
     <td><span><%= f.text_field :last_name_kana %></span><%= f.text_field :first_name_kana %></td>
     <td></td>
   </tr>
   <tr>
     <th>郵便番号</th>
     <td><%= f.text_field :postcode %></td>
   </tr>
   <tr>
     <th>住所</th>
     <td><%= f.text_field :address, size:80 %></td>
   </tr>
   <tr>
     <th>電話番号</th>
     <td><%= f.text_field :phone_number %></td>
   </tr>
   <tr>
     <th>メールアドレス</th>
     <td><%= f.email_field :email %></td>
   </tr>
   <tr>
    <th>会員ステータス</th>
    <td><span><%= f.radio_button :is_valid, true %>有効</span><%= f.radio_button :is_valid, false %>退会</td>
   </tr>
   <tr>
    <th></th>
    <td class="submit"><%= f.submit "変更を保存", class:"btn btn-success" %></td>
   </tr>

　<% end %>

 </table>
 </div>
 </div>
 </div>